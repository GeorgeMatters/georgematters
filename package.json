{
  "name": "georgematters",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "node scripts/check-node.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/check-node.mjs",
    "build": "astro build",
    "prepreview": "node scripts/check-node.mjs",
    "preview": "astro preview",
    "check": "node scripts/check-no-hardcoded-content.mjs && npm run build && node scripts/check-no-secrets.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/manrope": "^5.3.0",
    "astro": "^6.1.4",
    "js-yaml": "^4.1.1"
  }
}
